// set your configurations
var server = 'irc.freenode.net'
var channels = ['#unhosted']
var nick = 'bunuq'
var password = 'mr.unhosted.logbot'
var auth = false; // if no password, not registered, set to false.
var ircbot = true; // if you want to toggle off irc bot mode

// simple exports
exports.server = function() {
        return server;
}

exports.ircbot = function() {
        return ircbot;
}

exports.channels = function() {
        return channels;
}

exports.nick = function() {
        return nick;
}

exports.password = function() {
        return password
}

exports.auth = function() {
        return auth
}

